import { ImgenFondoPipe } from './imgen-fondo.pipe';

describe('ImgenFondoPipe', () => {
  it('create an instance', () => {
    const pipe = new ImgenFondoPipe();
    expect(pipe).toBeTruthy();
  });
});
