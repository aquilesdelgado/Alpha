import { ImgenDetallesPipe } from './imgen-detalles.pipe';

describe('ImgenDetallesPipe', () => {
  it('create an instance', () => {
    const pipe = new ImgenDetallesPipe();
    expect(pipe).toBeTruthy();
  });
});
