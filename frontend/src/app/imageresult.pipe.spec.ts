import { ImageresultPipe } from './imageresult.pipe';

describe('ImageresultPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageresultPipe();
    expect(pipe).toBeTruthy();
  });
});
